# Embeddings

# Embeddings can be both static or dynamic.

# static embeddings are values that never change, i.e. a constant.

# dynamic embeddings are values that do change

# - cron:
#     changes on a cron job
# - dependent:
#     calculate at call time
